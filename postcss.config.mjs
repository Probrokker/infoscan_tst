// PostCSS-конфиг для Tailwind v4. В v4 PostCSS-плагин один и сам подхватывает
// настройки из @theme внутри globals.css.

const postcssConfig = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default postcssConfig
